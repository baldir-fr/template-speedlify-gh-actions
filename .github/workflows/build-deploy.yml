name: Deploy Pages

on:
  push:
    branches: ["live"]
  # Allows the action to be triggered manually from Github "Actions" tab
  workflow_dispatch:
  workflow_run:
    # This action is triggered when the "Speedlify — scan & publish" is completed
    workflows: ["Speedlify — scan & publish"]
    types: 
      - "completed"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Install npm dependencies
        run: npm install
      - name: Build speedlify
        run: |
          npm run build -- --pathprefix=${GITHUB_REPOSITORY#*/}
          npm run zip-results
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
